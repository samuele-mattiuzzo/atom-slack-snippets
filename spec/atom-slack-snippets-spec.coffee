describe "when it's activated", ->
  it "should read the token", ->
    expect("apples").toEqual("apples")

describe "when a post is fired", ->
  it "should post to the view", ->
    expect("apples").toEqual("apples")
